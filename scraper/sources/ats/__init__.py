"""Public ATS board sources (Greenhouse / Lever / Ashby).

ATS boards are per-company, not searchable — the board tokens/handles live in
``scraper/companies.yml``. A dead/404 token logs a warning and is skipped; it
never fails the run.
"""
